{
  "schema_version": "1.0.0",
  "domain": "material_tools",
  "min_plugin_version": "1.0.0",
  "tools": [
    {
      "name": "create_material",
      "description": "Create a new Material asset with material expressions and connections.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path, e.g. /Game/Materials/M_Ground"
          },
          "expressions": {
            "type": "array",
            "description": "Material expressions to add",
            "items": {
              "type": "object",
              "properties": {
                "type": {
                  "type": "string",
                  "description": "Expression type: TextureSample, Constant, Constant3Vector, Color, Multiply, Lerp, LinearInterpolate, ScalarParameter, VectorParameter, TextureCoordinate"
                },
                "x": {
                  "type": "integer",
                  "description": "Node X position in graph"
                },
                "y": {
                  "type": "integer",
                  "description": "Node Y position in graph"
                },
                "value": {
                  "type": "number",
                  "description": "Value for Constant expressions"
                },
                "parameter_name": {
                  "type": "string",
                  "description": "Name for parameter expressions"
                },
                "connect_to": {
                  "type": "string",
                  "description": "Material property to connect to: BaseColor, Metallic, Specular, Roughness, EmissiveColor, Normal, Opacity, OpacityMask"
                }
              },
              "required": [
                "type"
              ]
            }
          }
        },
        "required": [
          "asset_path"
        ]
      }
    },
    {
      "name": "create_material_instance",
      "description": "Create a Material Instance from a parent material with parameter overrides.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path for the new MI"
          },
          "parent_material": {
            "type": "string",
            "description": "Content path of the parent Material"
          },
          "scalar_parameters": {
            "type": "object",
            "description": "Scalar parameter overrides: {\"Roughness\": 0.5}"
          },
          "vector_parameters": {
            "type": "object",
            "description": "Vector parameter overrides: {\"BaseColor\": {\"r\":1,\"g\":0,\"b\":0,\"a\":1}}"
          }
        },
        "required": [
          "asset_path",
          "parent_material"
        ]
      }
    },
    {
      "name": "add_material_expression",
      "description": "Add an expression to a material. (Deprecated: use create_material instead).",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path to the Material."
          }
        },
        "required": ["asset_path"]
      }
    },
    {
      "name": "connect_material_property",
      "description": "Connect an expression output to a material property. (Deprecated: use create_material instead).",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path to the Material."
          }
        },
        "required": ["asset_path"]
      }
    },
    {
      "name": "capture_material",
      "description": "Render the material to a texture and capture a snapshot image to disk.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Material to capture."
          },
          "max_dimension": {
            "type": "integer",
            "description": "Max width/height of the captured image."
          },
          "quality": {
            "type": "integer",
            "description": "JPEG quality (default: 75)."
          }
        },
        "required": ["asset_path"]
      }
    },
    {
      "name": "create_pbr_material_from_textures",
      "description": "Create a PBR Material asset from texture maps (Base Color, Normal, Roughness, Metallic, AO, etc.) and auto-wire shader pins.",
      "input_schema": {
        "type": "object",
        "properties": {
          "material_path": {
            "type": "string",
            "description": "Destination package path for Material asset (e.g. '/Game/Materials/M_CharacterPBR')"
          },
          "base_color_texture_path": {
            "type": "string",
            "description": "Object path of BaseColor texture (e.g. '/Game/Textures/T_Character_BC')"
          },
          "normal_texture_path": {
            "type": "string",
            "description": "Optional: Object path of Normal map texture"
          },
          "roughness_texture_path": {
            "type": "string",
            "description": "Optional: Object path of Roughness map texture"
          },
          "metallic_texture_path": {
            "type": "string",
            "description": "Optional: Object path of Metallic map texture"
          },
          "ao_texture_path": {
            "type": "string",
            "description": "Optional: Object path of Ambient Occlusion texture"
          },
          "specular_texture_path": {
            "type": "string",
            "description": "Optional: Object path of Specular texture"
          },
          "emissive_texture_path": {
            "type": "string",
            "description": "Optional: Object path of Emissive texture"
          },
          "opacity_texture_path": {
            "type": "string",
            "description": "Optional: Object path of Opacity texture"
          },
          "blend_mode": {
            "type": "string",
            "enum": ["Opaque", "Masked", "Translucent", "Additive"],
            "default": "Opaque"
          },
          "shading_model": {
            "type": "string",
            "enum": ["DefaultLit", "Unlit", "Subsurface", "ClearCoat"],
            "default": "DefaultLit"
          },
          "two_sided": {
            "type": "boolean",
            "default": false
          }
        },
        "required": [
          "material_path",
          "base_color_texture_path"
        ]
      }
    }
  ]
}