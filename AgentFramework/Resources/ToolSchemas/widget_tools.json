{
  "schema_version": "1.1.0",
  "domain": "widget_tools",
  "min_plugin_version": "1.0.0",
  "tools": [
    {
      "name": "create_widget_blueprint",
      "description": "Create a new UMG Widget Blueprint asset (WBP_) with an optional root widget class.\n\nIdempotent: if a Widget Blueprint already exists at asset_path it is reused and its existing widget tree and root widget are preserved (root_widget_class is ignored). Inspect it with get_widget_tree before adding widgets.\n\nWidget Blueprints have TWO systems (analogous to Blueprint actors):\n  1. Widget Tree: Visual hierarchy of UWidget objects (CanvasPanel, TextBlock, Button, etc.)\n     — managed via add_widget, set_widget_property, set_widget_slot\n  2. Widget Blueprint Graph: Standard K2 Blueprint logic (OnClicked bindings, variable set/get)\n     — populated via inject_blueprint_nodes_t3d with asset_path pointing to the WBP\n\nNaming convention: WBP_ prefix (e.g. WBP_HealthBar, WBP_MainMenu, WBP_Settings)\n\nWorkflow:\n1. create_widget_blueprint — create the asset with root panel\n2. add_widget — build the widget hierarchy (TextBlock, Button, Image, ProgressBar, etc.)\n3. set_widget_slot — configure layout: anchors, offsets, padding, alignment, fill rules\n4. set_widget_property — set text, colors, visibility, percent\n5. set_widget_font — configure fonts on text widgets\n6. set_widget_brush — configure images/brushes on Image/Button/Border widgets\n7. bind_widget_event — bind OnClicked, OnHovered, etc. to Blueprint functions\n8. compile_widget_blueprint — verify no errors\n9. inject_blueprint_nodes_t3d — wire click/hover event logic in the graph\n\nIMPORTANT: Rather than using steps 2 to 7 sequentially via separate tool calls, PREFER using 'instantiate_ui_hierarchy' to construct and configure the entire widget tree in a single, token-efficient transactional call.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path for the Widget Blueprint, e.g. /Game/UI/WBP_MainMenu. MUST start with /Game/ and be non-empty."
          },
          "root_widget_class": {
            "type": "string",
            "description": "Class for the root container widget. Default: CanvasPanel.\nOptions: CanvasPanel (freely positioned UI), VerticalBox (vertical auto-stack), HorizontalBox (horizontal auto-stack), SizeBox, Overlay (z-stacked layers), GridPanel, ScrollBox, WrapBox.\nUse CanvasPanel for HUD overlays with absolute positioning.\nUse VerticalBox/HorizontalBox for auto-layout menus and lists."
          },
          "parent_class": {
            "type": "string",
            "description": "Parent class for the Widget Blueprint. Default: UserWidget. Use 'CommonActivatableWidget' for full-screen menus with input routing (requires CommonUI plugin)."
          }
        },
        "required": ["asset_path"]
      }
    },

    {
      "name": "add_widget",
      "description": "Add a UMG widget to the widget tree of an existing Widget Blueprint.\n\nThe parent_widget MUST be a panel widget (a container that accepts children):\n\nPanel widgets (can be parents):\n  CanvasPanel — absolute positioning with anchors\n  VerticalBox — vertical auto-stack\n  HorizontalBox — horizontal auto-stack\n  GridPanel — grid layout\n  UniformGridPanel — uniform grid layout\n  ScrollBox — scrollable container\n  Overlay — z-stacked layers\n  WidgetSwitcher — tab-style visibility switcher\n  WrapBox — wrapping horizontal flow\n\nContent widgets (single child slot — can still be parent):\n  SizeBox — enforces min/max size on ONE child\n  ScaleBox — scales ONE child\n  Border — decorative border around ONE child\n  Button — clickable, contains ONE child (usually a TextBlock)\n  BackgroundBlur — blurred background for ONE child\n  InvalidationBox — caches rendering of ONE child\n  RetainerBox — render-to-texture for ONE child\n\nLeaf widgets (cannot be parents):\n  TextBlock — display text\n  RichTextBlock — styled text with markup\n  Image — display a texture/material\n  ProgressBar — fill-based value display\n  Slider — draggable numeric value\n  CheckBox — boolean toggle\n  EditableTextBox — single-line text input\n  MultiLineEditableTextBox — multi-line text input\n  ComboBoxString — dropdown selection\n  SpinBox — numeric spinner\n  Spacer — empty spacing\n  Throbber — animated loading dots\n  CircularThrobber — rotating loading indicator\n  ExpandableArea — collapsible section\n\nCall get_widget_tree FIRST to see current widget names and which are panels.\n\nIMPORTANT: After adding a widget to a CanvasPanel, you MUST call set_widget_slot to configure\nanchors, position, and size — otherwise the widget will have zero size and be invisible.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint to modify"
          },
          "widget_class": {
            "type": "string",
            "description": "Widget class to add. See tool description for full list of panels, content widgets, and leaf widgets."
          },
          "widget_name": {
            "type": "string",
            "description": "Unique name for the widget in the tree, e.g. TitleText, HealthBar, PlayButton. Used to reference this widget in other tools."
          },
          "parent_widget": {
            "type": "string",
            "description": "Name of the parent widget to add this to. Must be a panel or content widget. Use get_widget_tree to find names. If omitted, attaches to root panel."
          }
        },
        "required": ["asset_path", "widget_class", "widget_name"]
      }
    },

    {
      "name": "set_widget_slot",
      "description": "Configure the layout slot of a widget within its parent panel.\n\nEvery widget inside a panel has a SLOT that controls its layout. The slot type depends on the parent:\n\n## CanvasPanel Slots (UCanvasPanelSlot)\nFor widgets inside a CanvasPanel — control position, size, and anchoring:\n  anchors_min — FVector2D: anchor minimum (0,0)=top-left (0.5,0.5)=center (1,1)=bottom-right\n  anchors_max — FVector2D: anchor maximum. Same as min for fixed position; (1,1) for stretch-to-fill\n  offsets — FMargin: position and size offsets from anchor (Left=X, Top=Y, Right=Width, Bottom=Height when not stretching; all=padding when stretching)\n  alignment — FVector2D: pivot point (0.5,0.5)=center\n  auto_size — bool: auto-size to content\n  z_order — int: draw order (higher = on top)\n\nCommon anchor presets:\n  Fixed top-left: anchors_min=(0,0), anchors_max=(0,0), offsets=(X,Y,Width,Height)\n  Centered: anchors_min=(0.5,0.5), anchors_max=(0.5,0.5), alignment=(0.5,0.5)\n  Stretch-to-fill: anchors_min=(0,0), anchors_max=(1,1), offsets=(padding all sides)\n  Stretch horizontal: anchors_min=(0,0.5), anchors_max=(1,0.5)\n  Top bar: anchors_min=(0,0), anchors_max=(1,0), offsets=(0,0,0,Height)\n\n## VerticalBox / HorizontalBox Slots\n  padding — FMargin: space around widget (Left,Top,Right,Bottom)\n  size_rule — string: 'Auto' (shrink to content) or 'Fill' (expand to fill available space)\n  h_align — string: Left, Center, Right, Fill\n  v_align — string: Top, Center, Bottom, Fill\n\n## Overlay Slots\n  padding — FMargin\n  h_align — string: Left, Center, Right, Fill\n  v_align — string: Top, Center, Bottom, Fill\n\n## GridPanel Slots\n  row, column — int: grid cell position\n  row_span, column_span — int: how many cells to span\n  padding — FMargin\n  h_align, v_align — string\n\nIMPORTANT: This tool is MANDATORY when adding widgets to a CanvasPanel.\nWithout it, widgets have zero size and are invisible.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint"
          },
          "widget_name": {
            "type": "string",
            "description": "Exact name of the widget whose slot to configure"
          },
          "anchors_min": {
            "type": "string",
            "description": "Anchor minimum as 'X,Y' (CanvasPanel only). E.g. '0,0' for top-left, '0.5,0.5' for center, '0,0' with anchors_max='1,1' for stretch-to-fill."
          },
          "anchors_max": {
            "type": "string",
            "description": "Anchor maximum as 'X,Y' (CanvasPanel only). Same as anchors_min for fixed position. '1,1' for stretch."
          },
          "offsets": {
            "type": "string",
            "description": "Offsets as 'Left,Top,Right,Bottom' (CanvasPanel only). For fixed position: Left=X, Top=Y, Right=Width, Bottom=Height. For stretch: all values are padding."
          },
          "alignment": {
            "type": "string",
            "description": "Alignment pivot as 'X,Y' (CanvasPanel only). '0.5,0.5' centers the widget on its anchor point."
          },
          "auto_size": {
            "type": "boolean",
            "description": "Auto-size to content (CanvasPanel only). Default: false."
          },
          "z_order": {
            "type": "integer",
            "description": "Draw order (CanvasPanel only). Higher = drawn on top."
          },
          "padding": {
            "type": "string",
            "description": "Padding as 'Left,Top,Right,Bottom' or single value for uniform. (All slot types)"
          },
          "size_rule": {
            "type": "string",
            "description": "Size rule: 'Auto' or 'Fill'. (VerticalBox/HorizontalBox/ScrollBox slots)"
          },
          "h_align": {
            "type": "string",
            "description": "Horizontal alignment: 'Left', 'Center', 'Right', 'Fill'. (VerticalBox/HorizontalBox/Overlay/Grid slots)"
          },
          "v_align": {
            "type": "string",
            "description": "Vertical alignment: 'Top', 'Center', 'Bottom', 'Fill'. (VerticalBox/HorizontalBox/Overlay/Grid slots)"
          },
          "row": {
            "type": "integer",
            "description": "Grid row index (GridPanel/UniformGridPanel only)"
          },
          "column": {
            "type": "integer",
            "description": "Grid column index (GridPanel/UniformGridPanel only)"
          },
          "row_span": {
            "type": "integer",
            "description": "Number of rows to span (GridPanel only)"
          },
          "column_span": {
            "type": "integer",
            "description": "Number of columns to span (GridPanel only)"
          }
        },
        "required": ["asset_path", "widget_name"]
      }
    },

    {
      "name": "set_widget_slot_properties",
      "description": "Configure layout slot properties (anchors, alignment, offsets, padding, size rules, grid placement, z-order) on a child widget within a Widget Blueprint tree.\n\nOperates on UCanvasPanelSlot, UVerticalBoxSlot, UHorizontalBoxSlot, UOverlaySlot, UGridSlot, UUniformGridSlot, UScrollBoxSlot, UWrapBoxSlot, UWidgetSwitcherSlot, UScaleBoxSlot, UBorderSlot, and USizeBoxSlot.\nSupports structured parameter objects (anchors, alignment, offsets, padding, slot_properties) as well as flat parameters, accepting keys in both snake_case and PascalCase.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint, e.g. /Game/UI/WBP_MainMenu. Primary key. Alias for widget_blueprint_path / AssetPath / WidgetBlueprintPath."
          },
          "widget_blueprint_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint. Alias for asset_path."
          },
          "AssetPath": {
            "type": "string",
            "description": "Content path of the Widget Blueprint. PascalCase alias for asset_path."
          },
          "WidgetBlueprintPath": {
            "type": "string",
            "description": "Content path of the Widget Blueprint. PascalCase alias for widget_blueprint_path."
          },
          "widget_name": {
            "type": "string",
            "description": "Exact name of the target child widget inside the WidgetTree whose slot properties to modify, e.g. 'PlayButton', 'TitleText'. Primary key. Alias for WidgetName."
          },
          "WidgetName": {
            "type": "string",
            "description": "Exact name of the target child widget inside the WidgetTree. PascalCase alias for widget_name."
          },
          "slot_properties": {
            "type": "object",
            "description": "Optional nested container object holding layout slot properties (anchors, alignment, offsets, padding, size_rule, h_align, v_align, z_order, auto_size, row, column, row_span, column_span). Alias for SlotProperties / slot_params."
          },
          "SlotProperties": {
            "type": "object",
            "description": "Optional nested container object holding layout slot properties. PascalCase alias for slot_properties."
          },
          "anchors": {
            "type": "object",
            "description": "Anchor bounds object for CanvasPanelSlot (min_x, min_y, max_x, max_y). E.g. { 'min_x': 0, 'min_y': 0, 'max_x': 1, 'max_y': 1 } for stretch-to-fill, or { 'min_x': 0.5, 'min_y': 0.5, 'max_x': 0.5, 'max_y': 0.5 } for centered. Supports snake_case and PascalCase keys. Alias for Anchors.",
            "properties": {
              "min_x": { "type": "number", "description": "Minimum anchor X (0.0 to 1.0). Alias for MinX." },
              "MinX": { "type": "number", "description": "Minimum anchor X (0.0 to 1.0). PascalCase alias for min_x." },
              "min_y": { "type": "number", "description": "Minimum anchor Y (0.0 to 1.0). Alias for MinY." },
              "MinY": { "type": "number", "description": "Minimum anchor Y (0.0 to 1.0). PascalCase alias for min_y." },
              "max_x": { "type": "number", "description": "Maximum anchor X (0.0 to 1.0). Alias for MaxX." },
              "MaxX": { "type": "number", "description": "Maximum anchor X (0.0 to 1.0). PascalCase alias for max_x." },
              "max_y": { "type": "number", "description": "Maximum anchor Y (0.0 to 1.0). Alias for MaxY." },
              "MaxY": { "type": "number", "description": "Maximum anchor Y (0.0 to 1.0). PascalCase alias for max_y." }
            }
          },
          "Anchors": {
            "type": "object",
            "description": "Anchor bounds object for CanvasPanelSlot. PascalCase alias for anchors.",
            "properties": {
              "min_x": { "type": "number", "description": "Minimum anchor X (0.0 to 1.0). Alias for MinX." },
              "MinX": { "type": "number", "description": "Minimum anchor X (0.0 to 1.0). PascalCase alias for min_x." },
              "min_y": { "type": "number", "description": "Minimum anchor Y (0.0 to 1.0). Alias for MinY." },
              "MinY": { "type": "number", "description": "Minimum anchor Y (0.0 to 1.0). PascalCase alias for min_y." },
              "max_x": { "type": "number", "description": "Maximum anchor X (0.0 to 1.0). Alias for MaxX." },
              "MaxX": { "type": "number", "description": "Maximum anchor X (0.0 to 1.0). PascalCase alias for max_x." },
              "max_y": { "type": "number", "description": "Maximum anchor Y (0.0 to 1.0). Alias for MaxY." },
              "MaxY": { "type": "number", "description": "Maximum anchor Y (0.0 to 1.0). PascalCase alias for max_y." }
            }
          },
          "alignment": {
            "type": "object",
            "description": "Pivot alignment object (x, y) for CanvasPanelSlot (0.5,0.5 = center). E.g. { 'x': 0.5, 'y': 0.5 }. Supports snake_case and PascalCase keys. Alias for Alignment.",
            "properties": {
              "x": { "type": "number", "description": "X alignment pivot point (0.0=left, 0.5=center, 1.0=right). Alias for X." },
              "X": { "type": "number", "description": "X alignment pivot point. PascalCase alias for x." },
              "y": { "type": "number", "description": "Y alignment pivot point (0.0=top, 0.5=center, 1.0=bottom). Alias for Y." },
              "Y": { "type": "number", "description": "Y alignment pivot point. PascalCase alias for y." }
            }
          },
          "Alignment": {
            "type": "object",
            "description": "Pivot alignment object (x, y). PascalCase alias for alignment.",
            "properties": {
              "x": { "type": "number", "description": "X alignment pivot point (0.0=left, 0.5=center, 1.0=right). Alias for X." },
              "X": { "type": "number", "description": "X alignment pivot point. PascalCase alias for x." },
              "y": { "type": "number", "description": "Y alignment pivot point (0.0=top, 0.5=center, 1.0=bottom). Alias for Y." },
              "Y": { "type": "number", "description": "Y alignment pivot point. PascalCase alias for y." }
            }
          },
          "offsets": {
            "type": "object",
            "description": "Position and size offset margin object (left, top, right, bottom) for CanvasPanelSlot. For fixed position: left=X, top=Y, right=Width, bottom=Height. For stretch: padding from anchors. E.g. { 'left': 10, 'top': 20, 'right': 200, 'bottom': 50 }. Supports snake_case and PascalCase keys. Alias for Offsets.",
            "properties": {
              "left": { "type": "number", "description": "Left offset / X position. Alias for Left." },
              "Left": { "type": "number", "description": "Left offset / X position. PascalCase alias for left." },
              "top": { "type": "number", "description": "Top offset / Y position. Alias for Top." },
              "Top": { "type": "number", "description": "Top offset / Y position. PascalCase alias for top." },
              "right": { "type": "number", "description": "Right offset / Width. Alias for Right." },
              "Right": { "type": "number", "description": "Right offset / Width. PascalCase alias for right." },
              "bottom": { "type": "number", "description": "Bottom offset / Height. Alias for Bottom." },
              "Bottom": { "type": "number", "description": "Bottom offset / Height. PascalCase alias for bottom." }
            }
          },
          "Offsets": {
            "type": "object",
            "description": "Position and size offset margin object for CanvasPanelSlot. PascalCase alias for offsets.",
            "properties": {
              "left": { "type": "number", "description": "Left offset / X position. Alias for Left." },
              "Left": { "type": "number", "description": "Left offset / X position. PascalCase alias for left." },
              "top": { "type": "number", "description": "Top offset / Y position. Alias for Top." },
              "Top": { "type": "number", "description": "Top offset / Y position. PascalCase alias for top." },
              "right": { "type": "number", "description": "Right offset / Width. Alias for Right." },
              "Right": { "type": "number", "description": "Right offset / Width. PascalCase alias for right." },
              "bottom": { "type": "number", "description": "Bottom offset / Height. Alias for Bottom." },
              "Bottom": { "type": "number", "description": "Bottom offset / Height. PascalCase alias for bottom." }
            }
          },
          "padding": {
            "type": "object",
            "description": "Padding margin object (left, top, right, bottom) around widget for Box/Overlay/Grid slots. E.g. { 'left': 5, 'top': 5, 'right': 5, 'bottom': 5 }. Supports snake_case and PascalCase keys. Alias for Padding.",
            "properties": {
              "left": { "type": "number", "description": "Left padding. Alias for Left." },
              "Left": { "type": "number", "description": "Left padding. PascalCase alias for left." },
              "top": { "type": "number", "description": "Top padding. Alias for Top." },
              "Top": { "type": "number", "description": "Top padding. PascalCase alias for top." },
              "right": { "type": "number", "description": "Right padding. Alias for Right." },
              "Right": { "type": "number", "description": "Right padding. PascalCase alias for right." },
              "bottom": { "type": "number", "description": "Bottom padding. Alias for Bottom." },
              "Bottom": { "type": "number", "description": "Bottom padding. PascalCase alias for bottom." }
            }
          },
          "Padding": {
            "type": "object",
            "description": "Padding margin object around widget. PascalCase alias for padding.",
            "properties": {
              "left": { "type": "number", "description": "Left padding. Alias for Left." },
              "Left": { "type": "number", "description": "Left padding. PascalCase alias for left." },
              "top": { "type": "number", "description": "Top padding. Alias for Top." },
              "Top": { "type": "number", "description": "Top padding. PascalCase alias for top." },
              "right": { "type": "number", "description": "Right padding. Alias for Right." },
              "Right": { "type": "number", "description": "Right padding. PascalCase alias for right." },
              "bottom": { "type": "number", "description": "Bottom padding. Alias for Bottom." },
              "Bottom": { "type": "number", "description": "Bottom padding. PascalCase alias for bottom." }
            }
          },
          "size_rule": {
            "type": "string",
            "enum": ["Auto", "Fill"],
            "description": "Size rule for VerticalBox/HorizontalBox/ScrollBox slots: 'Auto' or 'Fill'. Alias for SizeRule."
          },
          "SizeRule": {
            "type": "string",
            "enum": ["Auto", "Fill"],
            "description": "Size rule for VerticalBox/HorizontalBox/ScrollBox slots. PascalCase alias for size_rule."
          },
          "h_align": {
            "type": "string",
            "enum": ["Left", "Center", "Right", "Fill"],
            "description": "Horizontal alignment: 'Left', 'Center', 'Right', 'Fill'. Alias for HAlign."
          },
          "HAlign": {
            "type": "string",
            "enum": ["Left", "Center", "Right", "Fill"],
            "description": "Horizontal alignment. PascalCase alias for h_align."
          },
          "v_align": {
            "type": "string",
            "enum": ["Top", "Center", "Bottom", "Fill"],
            "description": "Vertical alignment: 'Top', 'Center', 'Bottom', 'Fill'. Alias for VAlign."
          },
          "VAlign": {
            "type": "string",
            "enum": ["Top", "Center", "Bottom", "Fill"],
            "description": "Vertical alignment. PascalCase alias for v_align."
          },
          "z_order": {
            "type": "integer",
            "description": "Draw order for CanvasPanelSlot (higher = drawn on top). Alias for ZOrder."
          },
          "ZOrder": {
            "type": "integer",
            "description": "Draw order for CanvasPanelSlot. PascalCase alias for z_order."
          },
          "auto_size": {
            "type": "boolean",
            "description": "Auto-size to content for CanvasPanelSlot. Default: false. Alias for AutoSize."
          },
          "AutoSize": {
            "type": "boolean",
            "description": "Auto-size to content for CanvasPanelSlot. PascalCase alias for auto_size."
          },
          "row": {
            "type": "integer",
            "description": "Grid row index (0-based) for GridPanel/UniformGridPanel slots. Alias for Row."
          },
          "Row": {
            "type": "integer",
            "description": "Grid row index. PascalCase alias for row."
          },
          "column": {
            "type": "integer",
            "description": "Grid column index (0-based) for GridPanel/UniformGridPanel slots. Alias for Column."
          },
          "Column": {
            "type": "integer",
            "description": "Grid column index. PascalCase alias for column."
          },
          "row_span": {
            "type": "integer",
            "description": "Number of grid rows to span for GridPanel slots. Alias for RowSpan."
          },
          "RowSpan": {
            "type": "integer",
            "description": "Number of grid rows to span. PascalCase alias for row_span."
          },
          "column_span": {
            "type": "integer",
            "description": "Number of grid columns to span for GridPanel slots. Alias for ColumnSpan."
          },
          "ColumnSpan": {
            "type": "integer",
            "description": "Number of grid columns to span. PascalCase alias for column_span."
          },
          "fill_empty_space": {
            "type": "boolean",
            "description": "Fill empty space flag for WrapBox slots. Alias for FillEmptySpace."
          },
          "FillEmptySpace": {
            "type": "boolean",
            "description": "Fill empty space flag for WrapBox slots. PascalCase alias for fill_empty_space."
          }
        },
        "required": ["asset_path", "widget_name"]
      }
    },

    {
      "name": "set_widget_property",
      "description": "Set a property on a named widget inside a Widget Blueprint via C++ reflection.\n\nProperty names are exact C++ UPROPERTY names (case-sensitive). For complex types like fonts and brushes, prefer the dedicated set_widget_font and set_widget_brush tools.\n\nCommon properties by widget type:\n\nTextBlock:\n  Text — the displayed string (plain text, e.g. 'Hello World')\n  Justification — Left, Center, Right\n  AutoWrapText — true/false\n  WrapTextAt — float (0 = no wrap)\n  MinDesiredWidth — float\n  LineHeightPercentage — float (1.0 = default)\n  TextTransformPolicy — None, ToLower, ToUpper\n\nButton:\n  BackgroundColor — linear color: '(R=0.2,G=0.6,B=1.0,A=1.0)'\n  IsFocusable — true/false\n\nImage:\n  ColorAndOpacity — tint color: '(R=1.0,G=1.0,B=1.0,A=1.0)'\n\nProgressBar:\n  Percent — 0.0 to 1.0 fill value\n  FillColorAndOpacity — bar color\n  bIsMarquee — true/false for indeterminate progress\n\nSlider:\n  Value — 0.0 to 1.0\n  MinValue, MaxValue — range\n  StepSize — increment step\n  SliderBarColor, SliderHandleColor — colors\n\nCheckBox:\n  IsChecked — true/false\n\nEditableTextBox:\n  Text — initial text\n  HintText — placeholder text\n  IsPassword — true/false\n  IsReadOnly — true/false\n\nBorder:\n  ContentColorAndOpacity — content tint\n  BrushColor — border color\n  Padding — '(Left=5,Top=5,Right=5,Bottom=5)'\n\nSizeBox:\n  WidthOverride, HeightOverride — float (0 = no override)\n  MinDesiredWidth, MinDesiredHeight — float\n  MaxDesiredWidth, MaxDesiredHeight — float\n\nSpacer:\n  Size — '(X=20,Y=10)'\n\nAny widget:\n  Visibility — Visible, Collapsed, Hidden, HitTestInvisible, SelfHitTestInvisible\n  bIsEnabled — true/false\n  ToolTipText — hover tooltip string\n  RenderOpacity — 0.0 (transparent) to 1.0 (opaque)\n  Clipping — Inherit, ClipToBounds, ClipToBoundsWithoutIntersecting, ClipToBoundsAlways, OnDemand",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint"
          },
          "widget_name": {
            "type": "string",
            "description": "Exact name of the target widget (as shown in get_widget_tree)"
          },
          "property_name": {
            "type": "string",
            "description": "Exact C++ property name. Examples: Text, ColorAndOpacity, Visibility, Percent, bIsEnabled, BackgroundColor, Justification"
          },
          "property_value": {
            "type": "string",
            "description": "String-serialized value. Examples: 'Hello World' for text, '(R=1.0,G=0.5,B=0.0,A=1.0)' for color, 'Visible' for visibility, 'true'/'false' for booleans, '0.75' for floats"
          }
        },
        "required": ["asset_path", "widget_name", "property_name", "property_value"]
      }
    },

    {
      "name": "set_widget_font",
      "description": "Set the font on a text widget (TextBlock, EditableTextBox, RichTextBlock, MultiLineEditableTextBox).\n\nUE5 fonts use FSlateFontInfo which is complex to set via raw reflection. This tool provides a clean interface.\n\nAvailable engine fonts (no import needed):\n  Roboto — default UE font (Regular, Bold, Italic, Light, Medium, Black)\n  DroidSansMono — monospace font\n\nFor custom fonts, import a .ttf/.otf into the project first, then reference the asset path.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint"
          },
          "widget_name": {
            "type": "string",
            "description": "Name of the text widget to configure"
          },
          "font_family": {
            "type": "string",
            "description": "Font family name or asset path. Built-in: 'Roboto' (default). For custom fonts, use the asset path e.g. '/Game/Fonts/MyFont'"
          },
          "font_size": {
            "type": "integer",
            "description": "Font size in points. Common sizes: 12 (body), 16 (subtitle), 24 (title), 36 (heading), 48 (display). Default: 12"
          },
          "typeface": {
            "type": "string",
            "description": "Typeface style: Regular, Bold, Italic, Light, Medium, Black. Default: Regular"
          },
          "color": {
            "type": "string",
            "description": "Text color as '(R=1.0,G=1.0,B=1.0,A=1.0)'. Only for TextBlock (sets ColorAndOpacity)."
          },
          "shadow_offset": {
            "type": "string",
            "description": "Drop shadow offset as '(X=1,Y=1)'. Set to '(X=0,Y=0)' for no shadow."
          },
          "shadow_color": {
            "type": "string",
            "description": "Shadow color as '(R=0,G=0,B=0,A=0.5)'"
          }
        },
        "required": ["asset_path", "widget_name", "font_size"]
      }
    },

    {
      "name": "set_widget_brush",
      "description": "Set a brush (image/texture/solid color) on an Image, Button, Border, or ProgressBar widget.\n\nFSlateBrush is a complex struct that is difficult to set via raw reflection. This tool provides a clean interface.\n\nBrush targets by widget:\n  Image — 'Brush' (the displayed image)\n  Button — 'Normal', 'Hovered', 'Pressed', 'Disabled' (appearance states)\n  Border — 'Background' (border background)\n  ProgressBar — 'FillImage' (the fill bar brush)\n\nFor solid colors (no texture), just set tint_color and leave texture_path empty.\nFor textures, set texture_path to a Texture2D asset in the project.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint"
          },
          "widget_name": {
            "type": "string",
            "description": "Name of the widget to set brush on"
          },
          "brush_target": {
            "type": "string",
            "description": "Which brush to set. Image: 'Brush'. Button: 'Normal', 'Hovered', 'Pressed', 'Disabled'. Border: 'Background'. ProgressBar: 'FillImage'. Default: 'Brush'."
          },
          "texture_path": {
            "type": "string",
            "description": "Asset path of the texture, e.g. '/Game/Textures/T_ButtonNormal'. Leave empty for solid color."
          },
          "tint_color": {
            "type": "string",
            "description": "Tint/color as '(R=1.0,G=1.0,B=1.0,A=1.0)'. For solid color brushes (no texture), this IS the color."
          },
          "image_size": {
            "type": "string",
            "description": "Desired image size as '(X=64,Y=64)'. Affects the brush draw size."
          },
          "draw_as": {
            "type": "string",
            "description": "How to draw: 'Box' (9-slice), 'Border' (border only), 'Image' (stretch to fill), 'NoDrawType' (invisible). Default: Image."
          },
          "margin": {
            "type": "string",
            "description": "9-slice margin as '(Left=0.25,Top=0.25,Right=0.25,Bottom=0.25)'. Only used with draw_as=Box."
          }
        },
        "required": ["asset_path", "widget_name"]
      }
    },

    {
      "name": "bind_widget_event",
      "description": "Bind a widget event (OnClicked, OnHovered, etc.) to a custom event in the Widget Blueprint's EventGraph.\n\nThis creates a K2Node_ComponentBoundEvent node in the EventGraph that fires when the widget event occurs. After binding, you can use inject_blueprint_nodes_t3d to wire logic from the event node.\n\nSupported events by widget type:\n  Button: OnClicked, OnPressed, OnReleased, OnHovered, OnUnhovered\n  Slider: OnValueChanged, OnMouseCaptureBegin, OnMouseCaptureEnd\n  CheckBox: OnCheckStateChanged\n  EditableTextBox: OnTextChanged, OnTextCommitted\n  ComboBoxString: OnSelectionChanged, OnOpening\n  SpinBox: OnValueChanged, OnValueCommitted\n\nThe tool returns the created event node's name (e.g. 'K2Node_ComponentBoundEvent_0') which you can use as a starting point for inject_blueprint_nodes_t3d to wire subsequent logic.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint"
          },
          "widget_name": {
            "type": "string",
            "description": "Name of the widget to bind the event on (e.g. 'PlayButton')"
          },
          "event_name": {
            "type": "string",
            "description": "Name of the delegate event to bind. Button: OnClicked, OnPressed, OnReleased, OnHovered, OnUnhovered. Slider: OnValueChanged. CheckBox: OnCheckStateChanged. EditableTextBox: OnTextChanged, OnTextCommitted."
          }
        },
        "required": ["asset_path", "widget_name", "event_name"]
      }
    },

    {
      "name": "remove_widget",
      "description": "Remove a widget from the widget tree of a Widget Blueprint.\n\nIf the widget is a panel with children, all children are also removed.\nIf the widget is the root, the tree becomes empty (rootless).\nCannot be undone within this tool — use checkpoint restore to undo.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint"
          },
          "widget_name": {
            "type": "string",
            "description": "Name of the widget to remove"
          }
        },
        "required": ["asset_path", "widget_name"]
      }
    },

    {
      "name": "get_widget_tree",
      "description": "Read-only query: return the full widget hierarchy of a Widget Blueprint as JSON.\n\nReturns a list of all widgets with:\n  - name: exact widget name (use this in other widget tools)\n  - class: widget class name\n  - is_root: whether this is the root widget\n  - is_panel: true = can be used as parent_widget in add_widget\n  - child_count: number of direct children (for panels)\n  - slot_type: the slot class (e.g. CanvasPanelSlot, VerticalBoxSlot)\n  - parent: name of the parent widget (null for root)\n\nCall this BEFORE add_widget, set_widget_slot, or set_widget_property to know exact widget names and hierarchy.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint to inspect"
          }
        },
        "required": ["asset_path"]
      }
    },

    {
      "name": "compile_widget_blueprint",
      "description": "Compile a Widget Blueprint and return all errors and warnings.\n\nWidget Blueprints use the same compiler as regular Blueprint actors. Call after building the widget tree, setting slot properties, and wiring graph logic to verify correctness.\n\nAlso reports the widget count and root widget type for verification.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint to compile"
          }
        },
        "required": ["asset_path"]
      }
    },
    {
      "name": "macro_create_basic_ui_menu",
      "description": "Macro Tool: Create a basic UI menu Widget Blueprint in a single tool call.\n\nThis automatically creates a CanvasPanel root, a centered VerticalBox, a Title TextBlock, and a set of Buttons (with TextBlock labels) stacked vertically. It binds an empty OnClicked event for each button in the EventGraph so you can wire them up immediately with inject_blueprint_nodes_t3d.\n\nThis replaces calling create_widget_blueprint, add_widget x 10, set_widget_slot x 5, and bind_widget_event x N.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path for the new Widget Blueprint, e.g. /Game/UI/WBP_MainMenu"
          },
          "menu_title": {
            "type": "string",
            "description": "Text to display at the top of the menu (e.g. 'Main Menu')."
          },
          "button_names": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of strings for button labels (e.g. ['Play', 'Options', 'Quit']). The tool creates a button for each, named '<Name>Button'."
          }
        },
        "required": ["asset_path", "menu_title", "button_names"]
      }
    },
    {
      "name": "capture_widget",
      "description": "Capture a screenshot of a Widget Blueprint by rendering it headlessly (without opening the editor). The image is returned as a base64 data URI string directly in the tool result.\n\nThis is the PREFERRED METHOD for UI Visual Validation. It automatically measures the widget's dynamic desired size and renders it exactly to those bounds, producing a clean screenshot with no editor UI clutter.\n\nIf the widget is a full-screen layout (desired size is 0x0), it will fall back to the `resolution` parameter. You can also force a specific size using `render_size`.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint to capture, e.g. /Game/UI/WBP_MainMenu"
          },
          "render_size": {
            "type": "string",
            "description": "Optional forced render size as 'X,Y' (e.g. '1920,1080'). If provided, this overrides the widget's dynamic desired size."
          },
          "resolution": {
            "type": "string",
            "description": "Fallback resolution as 'X,Y' (e.g. '1920,1080') used ONLY if render_size is not provided AND the widget's desired size is 0x0 (e.g., a full-screen layout). Default: '1920,1080'"
          },
          "max_dimension": {
            "type": "integer",
            "description": "Maximum width or height in pixels to downscale the result to (256-2048, default 512). Keeps token count manageable."
          },
          "quality": {
            "type": "integer",
            "description": "JPEG quality (30-95, default 75). Use default unless higher fidelity is strictly required."
          }
        },
        "required": ["asset_path"]
      }
    },
    {
      "name": "instantiate_ui_hierarchy",
      "description": "Instantiate a complete UI hierarchy in a single transaction, constructing widgets, attaching them to parents, and configuring their properties, slots, fonts, brushes, and events. Strongly preferred over calling create_widget_blueprint + add_widget + set_widget_slot + set_widget_property + set_widget_font + set_widget_brush + bind_widget_event sequentially. This optimizes token usage, minimizes compilation cycles, and ensures all elements are correctly aligned and initialized at once.",
      "input_schema": {
        "type": "object",
        "properties": {
          "asset_path": {
            "type": "string",
            "description": "Content path of the Widget Blueprint to modify"
          },
          "widgets": {
            "type": "array",
            "description": "List of widgets to instantiate sequentially.",
            "items": {
              "type": "object",
              "properties": {
                "widget_class": {
                  "type": "string",
                  "description": "Class of the widget to create (e.g. TextBlock, Button, CanvasPanel)."
                },
                "widget_name": {
                  "type": "string",
                  "description": "Unique name for the widget instance."
                },
                "parent_widget": {
                  "type": "string",
                  "description": "Name of the parent widget to attach this widget to."
                },
                "properties": {
                  "type": "object",
                  "description": "Key-value pairs of properties to configure on the widget (e.g. {'Text': 'Submit'})."
                },
                "slot": {
                  "type": "object",
                  "description": "Layout slot settings (e.g. anchors, offsets, padding)."
                },
                "font": {
                  "type": "object",
                  "description": "Font parameters for text widgets (e.g. font_size, color)."
                },
                "brush": {
                  "type": "object",
                  "description": "Brush parameters for image/button/border widgets (e.g. tint_color, texture_path)."
                },
                "events": {
                  "type": "object",
                  "description": "Event bindings mapping delegate name (e.g. 'OnClicked') to custom function prefix (e.g. 'OnClicked_SubmitButton')."
                }
              },
              "required": ["widget_class", "widget_name"]
            }
          }
        },
        "required": ["asset_path", "widgets"]
      }
    }
  ]
}
