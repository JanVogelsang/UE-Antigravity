{
  "schema_version": "1.0.0",
  "domain": "settings_tools",
  "min_plugin_version": "1.0.0",
  "tools": [
    {
      "name": "read_config_value",
      "description": "Read a value from the project's INI configuration files.",
      "input_schema": {
        "type": "object",
        "properties": {
          "config_file": {
            "type": "string",
            "description": "Config filename: DefaultEngine, DefaultGame, DefaultEditor, DefaultInput"
          },
          "section": {
            "type": "string",
            "description": "INI section, e.g. /Script/Engine.RendererSettings"
          },
          "key": {
            "type": "string",
            "description": "Setting key name"
          }
        },
        "required": [
          "config_file",
          "section",
          "key"
        ]
      }
    },
    {
      "name": "write_config_value",
      "description": "Write a value to the project's INI configuration files.",
      "input_schema": {
        "type": "object",
        "properties": {
          "config_file": {
            "type": "string",
            "description": "Config filename: DefaultEngine, DefaultGame, DefaultEditor, DefaultInput"
          },
          "section": {
            "type": "string",
            "description": "INI section"
          },
          "key": {
            "type": "string",
            "description": "Setting key name"
          },
          "value": {
            "type": "string",
            "description": "New value to set"
          }
        },
        "required": [
          "config_file",
          "section",
          "key",
          "value"
        ]
      }
    },
    {
      "name": "macro_ensure_project_prerequisites",
      "description": "Macro Tool: Automatically ensures specific Unreal Engine plugins are enabled in the .uproject file.\n\nUse this tool BEFORE building systems that require specific plugins (e.g., EnhancedInput, PCG, CommonUI, GameplayAbilities). If a plugin is already enabled, it does nothing. If it enables a new plugin, it will prompt you to restart the Editor.",
      "input_schema": {
        "type": "object",
        "properties": {
          "plugins": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of exact plugin names to enable, e.g. ['EnhancedInput', 'PCG', 'CommonUI', 'GameplayAbilities']"
          }
        },
        "required": ["plugins"]
      }
    },
    {
      "name": "get_plugin_settings",
      "description": "Retrieve active AgentFramework Developer Settings (SecurityMode, protected paths, etc.).",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "list_config_sections",
      "description": "List all INI section headers present in a specified project config file.",
      "input_schema": {
        "type": "object",
        "properties": {
          "config_file": {
            "type": "string",
            "description": "Config filename: Engine, Game, Editor, Input, Scalability"
          }
        },
        "required": ["config_file"]
      }
    },
    {
      "name": "read_config_section",
      "description": "Read all key-value entries under a specified INI section in a config file.",
      "input_schema": {
        "type": "object",
        "properties": {
          "config_file": {
            "type": "string",
            "description": "Config filename: Engine, Game, Editor, Input, Scalability"
          },
          "section": {
            "type": "string",
            "description": "INI section name, e.g. /Script/Engine.RendererSettings"
          }
        },
        "required": ["section"]
      }
    }
  ]
}