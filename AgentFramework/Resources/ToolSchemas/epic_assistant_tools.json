{
  "schema_version": "1.0.0",
  "domain": "epic_assistant_tools",
  "min_plugin_version": "1.1.0",
  "tools": [
    {
      "name": "query_epic_assistant",
      "description": "Query Epic Games' native AI Assistant (introduced in Unreal Engine 5.7/5.8) to ask questions or perform tasks using Epic's internal documentation and assistant engine. Requires the AI Assistant tab to be open or available.",
      "input_schema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "The question or prompt to send to Epic's AI Assistant."
          }
        },
        "required": ["prompt"]
      }
    }
  ]
}
