{
  "image_support": false,
  "disabled_tools": [
    "capture_widget",
    "capture_viewport",
    "capture_niagara_system_isolated"
  ],
  "tool_overrides": {
    "execute_python_script": {
      "description": "Execute a Python script inside the Unreal Engine Editor's embedded Python interpreter. WARNING: This tool is a last resort. Do NOT use this tool if a native tool or dedicated skill exists. You must provide a detailed justification.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "reasoning_step": {
            "type": "string",
            "description": "Explain step-by-step what you are trying to achieve before writing the code."
          },
          "justification_why_native_tools_or_skills_are_insufficient": {
            "type": "string",
            "description": "Detailed explanation of why specific native tools or dedicated skills cannot be used. Python script usage is restricted to operations not supported by other tools/skills."
          },
          "script": {
            "type": "string",
            "description": "The exact Python code to execute. Must use the 'unreal' module."
          }
        },
        "required": ["reasoning_step", "justification_why_native_tools_or_skills_are_insufficient", "script"]
      }
    },
    "execute_batch_blueprint_operations": {
      "description": "Batch multiple Blueprint graph modifications (add components, variables, functions) in a single transaction, compiling once at the end. ALWAYS prefer this over making multiple individual calls. EXAMPLE: Plan out all nodes, variables, and connections before emitting the operations array.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "reasoning_step": {
            "type": "string",
            "description": "Step-by-step plan of which nodes to add and how they connect."
          },
          "blueprint_path": {
            "type": "string",
            "description": "The internal Unreal asset path to the Blueprint, e.g. '/Game/Blueprints/BP_MyActor'."
          },
          "operations": {
            "type": "array",
            "description": "Array of operation objects to execute in order."
          }
        },
        "required": ["reasoning_step", "blueprint_path", "operations"]
      }
    }
  }
}
